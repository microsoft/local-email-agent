# Local PostgreSQL Configuration (Docker)
STORAGE_MODE=local

# Local Database (no Azure credentials needed)
LOCAL_PGHOST=localhost
LOCAL_PGPORT=5432
LOCAL_PGDATABASE=emaildb
LOCAL_PGUSER=postgres
LOCAL_PGPASSWORD=P@ssw0rd!

# Local Blob Storage (using filesystem)
LOCAL_BLOB_PATH=./data/local_email_storage

# Azure OpenAI (still needed for embeddings and LLM)
AZURE_OPENAI_ENDPOINT=https://your-openai-resource.openai.azure.com/
